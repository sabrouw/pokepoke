import { BorderCardsDirective } from './border-cards.directive';

describe('BorderCardsDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderCardsDirective();
    expect(directive).toBeTruthy();
  });
});
